spring:
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles:
    active: development
    include: ${spring.application.name}-blacklist
  application:
    name: service-x
